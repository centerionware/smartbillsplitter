# Set default behavior for all files to let Git automatically handle line endings.
* text=auto

# Treat SVG files as text, as they are XML-based. This provides better diffs.
*.svg text

# Explicitly define common image files and fonts as binary to prevent corruption.
# This tells Git to not modify their line endings or attempt to generate text diffs.
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.webp   binary
*.ico    binary
*.woff   binary
*.woff2  binary
*.eot    binary
*.ttf    binary
*.otf    binary
