{
  "name": "smart-bill-splitter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "esbuild functions/scan-receipt.ts --bundle --platform=node --format=esm --outfile=netlify/functions/scan-receipt.mjs && esbuild functions/sync.ts --bundle --platform=node --format=esm --outfile=netlify/functions/sync.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "@google/genai": "^1.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "esbuild": "^0.23.0"
  }
}
